<!DOCTYPE html>
<html>
<head>
  <title>Music Quiz</title>
</head>
<body>
  <h1>🎶 Guess That Track!</h1>

  <p><strong>Genre:</strong> <%= quiz.genre %></p>

  <audio controls autoplay>
    <source 
    src="<%= quiz.audio_url.includes('p.scdn.co') 
      ? '/quiz/proxy-audio?url=' + encodeURIComponent(quiz.audio_url) 
      : quiz.audio_url %>" 
    type="audio/mpeg">
  
    Your browser does not support the audio element.
  </audio>

  <form action="/quiz/submit" method="POST">
    <input type="hidden" name="quiz_id" value="<%= quiz.id %>">

    <label for="artist">🎤 Who is the artist?</label><br>
    <input type="text" name="artist" required><br><br>

    <label for="title">🎵 What's the song title?</label><br>
    <input type="text" name="title" required><br><br>

    <label for="genre">🎧 What’s the genre?</label><br>
    <input type="text" name="genre" required><br><br>

    <button type="submit">Submit Answers</button>
  </form>
</body>
</html>
